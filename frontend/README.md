# Deprecated UI

This `frontend/` app is no longer used. The live dashboard is under `dashboards/client` (served by pm2 `leadforge-frontend`).

Keep this folder only for reference; do not build or deploy from here. New UI changes should be made in `dashboards/client/`.
